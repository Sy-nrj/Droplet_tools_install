#!/bin/bash

DOMAIN=$1

# Run subdomain enumeration
./enumerate_subdomains.sh $DOMAIN

# Run DNS resolution
./resolve_dns.sh $DOMAIN

# Run asset probing
./probe_assets.sh $DOMAIN

# Run vulnerability scanning
./scan_vulnerabilities.sh $DOMAIN